{% set name = "cabal" %}
{% set version = "3.0.2.0" %}
{% set sha256 = "2f12b39b81b7c8b3c99c0426c8f866dc90e258a22c08545528d88a86a72ac674" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - url: https://github.com/haskell/cabal/archive/Cabal-v{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - ghc-bootstrap
    - zlib
    - gmp
test:
  commands:
    - cabal --help

about:
  home: https://haskell.org/cabal/
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: cabal-install is the command line interface to Cabal and hackage.

  doc_url: https://www.haskell.org/cabal/index.html
  dev_url: https://github.com/haskell/cabal/tree/master/cabal-install

extra:
  recipe-maintainers:
    - eggzilla
