{% set name = "cabal" %}
{% set version = "3.8.1.0" %}
{% set sha256 = "d4eff9c1fcc5212360afac8d97da83b3aff79365490a449e9c47d3988c14b6bc" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  - url: https://github.com/haskell/cabal/archive/Cabal-v{{ version }}.tar.gz
    sha256: {{ sha256 }}

build:
  number: 0
  skip: true  # [not linux]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - ghc-bootstrap
    - zlib
    - gmp
test:
  commands:
    - cabal --help

about:
  home: https://haskell.org/cabal/
  license: BSD 3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: cabal-install is the command line interface to Cabal and hackage.

  doc_url: https://www.haskell.org/cabal/index.html
  dev_url: https://github.com/haskell/cabal/tree/master/cabal-install

extra:
  recipe-maintainers:
    - eggzilla
